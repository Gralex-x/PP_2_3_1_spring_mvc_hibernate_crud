version: '3.7'

services:
  mysql:
    image: mysql:8
    container_name: spring_mvc_hibernate
    environment:
      MYSQL_DATABASE: 'spring_mvc_hibernate'
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - "3306:3306"